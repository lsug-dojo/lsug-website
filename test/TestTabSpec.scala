import org.specs2.mutable._

import play.api.test._
import play.api.test.Helpers._

class TestTabSpec extends Specification {

  "run in a browser" in {
    
    /*
    running(TestServer(3333), HTMLUNIT) { browser =>
    
    browser.goTo("http://localhost:3333")
    //browser.$("#title").first().getText()  must equalTo("Welcome to London Scala User Group")
    browser.title must equalTo("Welcome to London Scala User Group")

    }
     */
  done
  }

}
